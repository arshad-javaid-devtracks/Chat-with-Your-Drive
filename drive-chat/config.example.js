// Copy this file to config.js and set your webhook URL
// Example: cp config.example.js config.js

const CHAT_PORTAL_CONFIG = {
  // n8n Respond to Webhook endpoint
  webhookUrl: '',
  // Optional: if you protect your webhook with a gateway
  apiKey: '',
};
